using Form.LabelWindow;

namespace Form.SpecialThanks
{
    public class SpecialThanks : LabelWindowContent
    {
        // Start is called before the first frame update
        private void Start()
        {
        }

        // Update is called once per frame
        private void Update()
        {
        }
    }
}