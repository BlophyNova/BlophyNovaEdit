using Scenes.PublicScripts;
using TMPro;
using UnityEngine;

namespace Form.QuickOpen
{
    public class QuickOpenItem : PublicButton
    {
        public TextMeshProUGUI text;
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
